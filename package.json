{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:tenant": "pnpm --filter tenant dev",
    "dev:management": "pnpm --filter management dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0"
  }
}